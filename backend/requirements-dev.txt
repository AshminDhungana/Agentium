# Development-only dependencies for Agentium backend
# Production deps are in requirements.txt; this file lists explicit testing tools.
pytest==8.3.4
pytest-asyncio==0.25.0
pytest-cov==6.0.0
pytest-env==1.1.5
factory-boy==3.3.1
faker==33.3.0

# --- Performance Testing  ---
locust>=2.29
pytest-benchmark>=4.0
py-cpuinfo>=9.0

# --- Redis test double (Phase 18.1 Security Suite) ---
# [lua] extra is required: RateLimitMiddleware uses script_load/evalsha,
# and without lupa installed fakeredis can't execute the Lua sliding-window
# script, silently degrading every test to the native-pipeline fallback path.
fakeredis[lua]>=2.26

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 📖 DOCSTRING COVERAGE
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
interrogate>=1.7.0

# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 🔐 SECRET SCANNING (Phase 18.5)
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
detect-secrets>=1.5.0